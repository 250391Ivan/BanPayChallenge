{
  "name": "banpaychallenge",
  "version": "1.0.0",
  "description": "Challenge  make Endpoint",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dotenv nodemon ./index.js",
    "migrate:up": "dotenv npx sequelize-cli db:migrate",
    "migrate:undo:all": "dotenv npx sequelize-cli db:migrate:undo:all",
    "seed:all": "dotenv npx sequelize-cli db:seed:all",
    "seed:undo": "dotenv npx sequelize-cli db:seed:undo:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.3.0",
    "express": "^4.18.2",
    "mariadb": "^3.2.3",
    "mysql2": "^3.7.0",
    "sequelize": "^6.35.2",
    "swagger-ui-express": "^5.0.0",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
